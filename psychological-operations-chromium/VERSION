# Per-platform Chromium Browser Snapshot revisions.
#
# Chromium snapshots are continuous-build-per-commit, with each platform
# pipeline running independently — a given revision may exist for some
# platforms but not others. Pin one rev per upstream-platform directory.
#
# Each entry must be a rev whose chrome-<short>.zip is published at
#   commondatastorage.googleapis.com/chromium-browser-snapshots/<plat>/<rev>/
# Bump by hitting `<plat>/LAST_CHANGE` and confirming the artifact exists.
WIN_X64=1627931
WIN=1627905
LINUX_X64=1627938
MAC=1627912
MAC_ARM=1627944
