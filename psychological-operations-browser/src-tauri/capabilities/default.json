{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "X-App window capability. Targets both child webviews (panel + content). The panel is local (tauri:// scheme) so IPC works unconditionally; the content webview is remote (https://console.x.ai/ etc.) and uses the `remote.urls` allowlist to enable IPC from that origin.",
  "windows": ["x-app"],
  "webviews": ["panel", "content"],
  "remote": {
    "urls": [
      "https://x.com/*",
      "https://*.x.com/*",
      "https://console.x.ai/*"
    ]
  },
  "permissions": [
    "core:default",
    "allow-frontend-ready",
    "allow-stdio-respond",
    "allow-current-mode",
    "allow-current-signed-in",
    "allow-current-panel",
    "allow-report-url"
  ]
}
