{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "X-App window capability. Targets both child webviews (panel + content). The panel is local (tauri:// scheme) so IPC works unconditionally; the content webview is remote (https://console.x.com/ etc.) and uses the `remote.urls` allowlist to enable IPC from that origin.",
  "windows": ["x-app"],
  "webviews": ["panel", "content"],
  "remote": {
    "urls": [
      "https://x.com/*",
      "https://*.x.com/*"
    ]
  },
  "permissions": [
    "core:default",
    "allow-frontend-ready",
    "allow-stdio-respond",
    "allow-current-mode",
    "allow-current-signed-in",
    "allow-current-panel",
    "allow-report-url",
    "allow-set-production-app-count",
    "allow-store-x-app-credential"
  ]
}
