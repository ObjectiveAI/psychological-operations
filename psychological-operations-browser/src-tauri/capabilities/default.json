{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "X-App window capability. The panel webview is local (tauri:// scheme) so Tauri IPC works unconditionally for it. The content area is a CEF browser (not a Tauri webview, no Tauri IPC, no Tauri events) — the overlay running inside it uses the `psyops://` custom scheme handler in `cef_scheme.rs` to call back into Rust.",
  "windows": ["x-app"],
  "webviews": ["panel"],
  "permissions": [
    "core:default",
    "allow-current-panel"
  ]
}
