"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-frontend-ready"
description = "Allows the injected overlay to signal that its `psyops:request` listener is registered, unblocking the Rust stdin reader."
commands.allow = ["frontend_ready"]
