"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-stdio-respond"
description = "Allows the injected overlay to fulfill an in-flight stdio request with a Response (Ack or err)."
commands.allow = ["stdio_respond"]
